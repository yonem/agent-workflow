# スレッド運用ルール

## 目的

複数の作業ループを同時に運用できるよう、タスク固有の入力と結果をスレッド単位で分離する。

## 構成

- 各スレッドは `threads/` 配下に、固有のスレッド名を持つディレクトリとして配置する
- スレッド名は小文字の英数字とハイフンで構成し、`rules/`、`worker-definitions/` などの共有ディレクトリ名と重複させない
- 標準スレッドは `normal/`、緊急対応スレッドは `emergency/` とする
- `normal`、`emergency` などの名称は用途を表すものであり、スレッド間に優先順位は設けない
- 将来のスレッド追加では、`threads/` 配下に同じ構成のディレクトリを追加する
- 1つのスレッドで扱うタスクは常に1つとし、同一スレッドで複数タスクを並行して実行しない

## スレッドとプロジェクトの対応

スレッドとCodexプロジェクトは1対1で対応させる。

- 1つのスレッドに割り当てるCodexプロジェクトは1つだけとする
- 1つのCodexプロジェクトを複数のスレッドで共有しない
- `current-task.md` に、そのスレッド専用の `CodexプロジェクトID` を必ず記載する
- `current-task.md` に `Codex実行ディレクトリ` と `対象リポジトリ` を分けて記載する
- workerは自身の `projectId` と `current-task.md` の `CodexプロジェクトID` を照合する
- `projectId` が未設定、取得不能、不一致の場合は作業を開始しない
- Ownerはworker接続前に、全workerが対象スレッドの `projectId` に所属していることを確認する
- プロジェクトの紐付けを確認できないworkerは、正しいプロジェクトで再作成または再接続してから作業を開始する

```text
threads/<thread-name>/
├── docs/
│   └── current-task.md
└── result/
    ├── plan.md
    ├── changes.md
    ├── test.md
    ├── security.md
    ├── review.md
    └── task-log.md
```

## ファイルの責務

- `threads/<thread-name>/docs/current-task.md` は、そのスレッドでPlannerへ渡す現在のタスクとする
- `threads/<thread-name>/result/task-log.md` は、Documenterが他workerと同じresult領域へタスク固有の判断、結果、残課題を記録し、タスクごとに上書きする
- `threads/<thread-name>/result/` は、そのスレッドのworker間で受け渡すタスク固有の結果を格納する
- `result/` 内はworker別に分割せず、固定ファイル名をタスクごとに上書きする
- ルートの `development-improvement.md` は全スレッドで共有し、汎用的な改善項目だけを永続化する
- ルートの `rules/` と `worker-definitions/` は全スレッドで共有する

## 共有ファイル更新時の注意

- 共有ファイルを更新する前に、別スレッドで更新中でないか、最新内容を確認する
- ルートの `development-improvement.md` や `rules/` 配下を更新する場合は、同時に書き込まないように作業順を調整する
- 競合や相手の更新を検知した場合は、内容を統合してから保存し、判断できない差分はOwnerへ報告する
- スレッド固有の結果は共有ファイルへ書き込まず、対象スレッドの `result/` に保存する

## workerの参照先

- workerは、現在処理しているスレッドの `threads/<thread-name>/docs/current-task.md` と `threads/<thread-name>/result/` を参照する
- Ownerは作業対象のスレッド名とタスクを、そのスレッドの `current-task.md` に記載する
- Ownerは `current-task.md` に次の6項目を必ず記載する：`スレッド名`、`CodexプロジェクトID`、`Codex実行ディレクトリ`、`対象リポジトリ`、`ベースブランチ`、`作業ブランチ`
- workerは作業開始時に自身のCodexプロジェクトID、Codex実行ディレクトリ、対象リポジトリへのアクセス可否を確認する。Codex実行ディレクトリは対象リポジトリと一致しなくてもよい。不一致または確認不能の場合は作業を開始せず、実際の値と期待値を親タスクへ報告する
- workerは作業開始時に `current-task.md` のスレッド名が自身に割り当てられたスレッドと一致することを確認し、一致しない場合は作業を開始せず親タスクへ報告する
- `worker-definitions/` の定義に記載するパスは、特定のスレッド名に固定せず `threads/<thread-name>/` で表現する
- workerは開始時に共有ルールと対象スレッドの入力を確認し、結果を同じスレッドの `threads/<thread-name>/result/` に保存する
- スレッド間で結果ファイルを直接共有せず、必要な内容は親タスクが確認して対象スレッドへ引き継ぐ

## 新規スレッド作成時の手順

Ownerは新しいスレッドを作成するとき、次の順序で専用プロジェクトとworkerを準備する。

```text
スレッド名を決定
  ↓
専用Codexプロジェクトを作成
  ↓
projectIdを取得
  ↓
current-task.mdへprojectIdを記載
  ↓
workerチャットを同じプロジェクトへ作成
  ↓
全workerのprojectIdを照合
  ↓
Plannerを起動
```

projectIdの照合が完了するまで、Plannerを含むworkerを起動しない。

## 作業中の共通ルール変更

- タスク開始時に、その時点で適用される `rules/` を確認する
- 作業中に `rules/` または `worker-definitions/` が変更された場合、次のworkerへ接続する前に親タスクがタスクへの影響を確認する
- 影響がある場合は、影響範囲、再確認が必要なworker、Owner判断事項を記録し、必要な再検証が完了するまで次工程へ接続しない
- 影響がない場合も、変更内容と影響なしの判断を親タスクの記録へ残す

## 同期失敗時の復旧

Codexの再起動後などにworkerチャットの表示、プロジェクト所属、または対象リポジトリへのアクセスが不一致になった場合は、Ownerが復旧を行う。workerは自身のプロジェクト所属を修正する前提にせず、不一致を検知した時点で作業を停止する。

Ownerは次の手順でworkerを再接続する。

1. 対象プロジェクト、対象スレッド、対象リポジトリを確認する
2. 既存workerチャットの重複、所属不明、誤った作業ディレクトリを確認する
3. 必要なworkerを役割名で再作成する
4. 全workerを同じプロジェクトへ設定し、対象リポジトリへアクセスできることを確認する
5. 各workerのprojectId、Codex実行ディレクトリ、対象リポジトリへのアクセス、スレッド名、ブランチを照合する
6. `current-task.md` と既存のresultファイルを入力として、Plannerから通常の承認フローを再開する

再接続時は、次の項目を確認する。

```text
□ チャット名が役割名である
□ 全workerが同じプロジェクトに所属している
□ projectIdが一致している
□ Codex実行ディレクトリを記録している
□ 対象リポジトリへアクセスできる
□ 意図しないworktreeを作成していない
□ current-task.mdの6項目が一致している
□ 既存のresultファイルを確認している
```

会話履歴が復元できない場合も、`current-task.md` と同じスレッドの `result/` を正として作業を再開する。再作成したworkerは、復旧したこと、参照した結果ファイル、未確認事項、Owner判断を親タスクへ報告する。

## タスクと再検証の境界

- Ownerが同じスレッドの `current-task.md` に同一のタスク識別情報を記載し、前回の指摘・修正に対する確認を続ける場合は同一タスクの再検証とする
- 同一タスクの再検証では、既存の結果ファイルと `result/task-log.md` を削除・上書きせず、差分、解消内容、追加事項を追記する
- タスク識別情報、目的、対象範囲のいずれかが変わる場合は新規タスクとする
- 新規タスクでは、結果ファイルと `result/task-log.md` を新しいタスクの内容で上書きし、Owner判断の記録も新しい表として開始する
- Ownerが新規タスクへ切り替える場合は、前タスクの最終判定、未確認事項、Owner判断 (追記) の末尾が `なし` であることを確認してから `current-task.md` を更新する
- `Owner判断` と `Owner判断 (追記)` の `なし` は、そのworkerのその報告時点に判断事項がないことを示す。過去の判断履歴を削除したり、将来の判断発生を妨げたりするものではない

## 追加・終了

- 新しいスレッドを追加するときは、`threads/<new-thread-name>/` に既存スレッドと同じ `docs/` と `result/` の構成を作成する
- スレッド固有のルールはそのスレッド内に作らず、共通化できる場合は `rules/` に追加する
- スレッドを終了しても、共有ルールとルートの改善記録は削除しない
